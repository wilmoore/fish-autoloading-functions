> fish autoloads functions, by looking for a file with the name of that command in `~/.config/fish/functions/`.

```
git clone git@github.com:wilmoore/fish-autoloading-functions.git ~/.config/fish/functions
```
